> clonar repositório
> npm install / npm i para instalar as dependências (apenas primeiro acesso após baixar o repositório)

Para pegar sku leve aleatório:
npm run leve

Para pegar sku pesado aleatório:
npm run pesado

Para pegar sku liquido aleatório:
npm run liquido
